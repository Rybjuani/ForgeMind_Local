PyQt6>=6.7
psutil>=5.9
# Opcionales (NO instalar si no los queres):
#   llama-cpp-python  -> solo si vas a usar backend_kind = "llama_cpp" (binding Python)
#   PyInstaller       -> solo si queres empaquetar a .exe mas adelante